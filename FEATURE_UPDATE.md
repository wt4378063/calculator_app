# 批量求和计算功能说明

## 新增功能：批量求和计算（多数字 + 单价）

从普通计算器主界面底部点击 **「批量求和计算（多数字+单价）」** 按钮进入。

### 使用步骤

1. **输入单价**：在顶部「单价」输入框填写单价
2. **逐个添加数字**：在输入框中输入数字，点击「添加」，可反复添加多个
   - 已添加的数字会显示在中央列表区域（含当前累加和）
3. **点击「计算」**，即可看到 4 项结果：
   - **加法总和**：所有已添加数字的累加和
   - **总和 × 单价**：加法总和乘以单价的结果
   - **总和平均值**：加法总和 ÷ 数字个数
   - **平均值 × 单价**：平均值乘以单价（高亮显示）
4. 点「清空」可重置，点「返回普通计算器」回到主界面

### 示例

输入单价 `5`，依次添加 `10`、`20`、`30`：

| 结果项 | 值 |
|---|---|
| 加法总和 | 60 |
| 总和 × 单价 | 300 |
| 总和平均值 | 20 |
| 平均值 × 单价 | 100 |

### 技术说明

- 新 Activity：`BatchCalculateActivity`
- 新布局：`res/layout/activity_batch.xml`
- 计算逻辑已做浮点精度处理（0.1+0.2+0.3=0.6，不会显示 0.6000000000000001）
- 核心逻辑抽离为纯函数，已编写 16 项单元测试验证（见 `app/src/test/BatchCalculateLogicTest.kt`）

### 生成 APK

方式一（推荐）：push 到 GitHub → Actions → Run workflow → 下载 APK

方式二：Android Studio → Build → Build APK(s)
